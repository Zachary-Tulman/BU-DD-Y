BU-DD-Y (pronounced "Buddy") is a voice-activated AI chat assistant that you can converse with in real time.

This project uses Vosk speech recognition to convert user voice input to legible text. That text is then sent to an LLM API of choice. The API response is then spoken via pyttsx3 text to speech and the bot awaits further voice input.
